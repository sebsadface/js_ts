/** Calculates (incorrectly) the value (n-1)^2. */
export function quadratic1(n: number): number {
  return -1;  // TODO: change this
}

/** Calculates (incorrectly) the value (n-1)^2. */
export function quadratic2(n: number): number {
  return -1;  // TODO: change this
}


/** Returns x if b is true and y if b is false. */
function cond(b: boolean, x: number, y: number): number {
  if (b) {
    return x;
  } else {
    return y;
  }
}

/** Calculates (incorrectly) the value |x|.  */
export function abs_value1(x: number): number {
  return 0;  // TODO: change this to use cond
}

/** Calculates (incorrectly) the value |x|.  */
export function abs_value2(x: number): number {
  return 0;  // TODO: change this to use "_ ? _ : _"
}
